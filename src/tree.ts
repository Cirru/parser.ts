import { ICirruNode } from "./types";

let addToList = (acc: ICirruNode[], xs: ICirruNode[]) => {
  return acc.concat(xs);
};

export let commaHelper = (before: ICirruNode[], after: ICirruNode[]) => {
  // TODO
};

export let dollarHelper = (before: ICirruNode[], after: ICirruNode[]) => {
  // TODO
};
